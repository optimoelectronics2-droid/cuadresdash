[build]
  command = "npm run build:netlify"
  publish = ".next"

[build.environment]
  NPM_FLAGS = "--legacy-peer-deps"
  NODE_VERSION = "20"

# Permite que el service worker controle toda la app
[[headers]]
  for = "/sw.js"
  [headers.values]
    Service-Worker-Allowed = "/"
